node_modules/
.next/
.env
.env.local
.env*.local
*.log
.DS_Store
coverage/
playwright-report/
test-results/

# build output
.next/
next-env.d.ts
*.tsbuildinfo

# local test cluster (scripts/test-db.sh)
.testdb/

# Branding source under consideration, not an asset yet (§14 feature 13).
# The file stays on disk to be looked at; it is not committed while it is
# undecided. Today's copy is a 500x302 JPEG misnamed .png, which is too weak
# a source for a 16px tile — see §14.6.
logo.png
